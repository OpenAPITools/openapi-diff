openapi: 3.0.0
info:
  title: Projects API
  version: 1.0.0
paths:
  /pet/{petId}:
    get:
      parameters:
        - name: petId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "4XX":
          description: "Invalid input"
