openapi: 3.0.2
info:
  description: Test for null schema issue
  title: Null Schema Test
  version: 1.0.0
paths:
  /test:
    post:
      requestBody:
        content:
          application/json:
            example: updated example
            schema:
              type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: string
