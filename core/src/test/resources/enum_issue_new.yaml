openapi: 3.0.0
info:
  title: Enum Issue Test
  version: 1.0.0
paths:
  /test:
    get:
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SomeStatus'
components:
  schemas:
    SomeStatus:
      description: Some status description
      enum:
        - StatusOne
        - StatusTwo
        - StatusNew
      type: string
      x-ms-enum:
        name: SomeStatus
        modelAsString: false
        values:
          - name: StatusOne
            value: StatusOne
          - name: StatusTwo
            value: StatusTwo
          - name: StatusNew
            value: StatusNew